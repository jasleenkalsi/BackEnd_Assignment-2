export interface Employee {
    id: string;  // Unique ID for the employee
    name: string;  // Name of the employee
    position: string;  // Position/job title of the employee
    department: string;  // Department where the employee works
    email: string;  // Employee's email address
    phone: string;  // Employee's phone number
    branchId: string;  // ID of the branch the employee is assigned to
  }
  